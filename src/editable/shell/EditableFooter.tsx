'use client'
import Link from 'next/link'
import { SITE_CONFIG } from '@/lib/site-config'
import { useEditableLocalAuthSession } from '@/editable/components/EditableLocalAuthForms'
export function EditableFooter() { const { session, logout }=useEditableLocalAuthSession(); const year=new Date().getFullYear(); return <footer className="bg-[var(--editable-footer-bg)] text-[var(--editable-footer-text)]"><div className="mx-auto grid max-w-[var(--editable-container)] gap-12 px-5 py-16 sm:px-8 md:grid-cols-2 lg:px-10"><div><Link href="/" className="editable-display text-3xl font-bold tracking-[-.07em]">{SITE_CONFIG.name}</Link><p className="mt-5 max-w-xs text-sm leading-6 text-white/60">A living catalogue of useful things, gathered with care.</p></div><div><p className="text-[10px] font-bold uppercase tracking-[.2em] text-white/50">Elsewhere</p><div className="mt-5 grid gap-3 text-sm"><Link href="/about">About</Link><Link href="/contact">Contact</Link>{session ? <><Link href="/create">Submit a resource</Link><button onClick={logout} className="text-left">Log out</button></> : <><Link href="/login">Log in</Link><Link href="/signup">Join</Link></>}</div></div></div><div className="border-t border-white/15 px-5 py-5 text-center text-[10px] uppercase tracking-[.16em] text-white/45">© {year} {SITE_CONFIG.name}</div></footer> }
